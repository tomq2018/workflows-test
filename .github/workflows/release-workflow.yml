# This is a basic workflow to help you get started with Actions

name: CI-RELEASE

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/staging/') && !startsWith(github.ref, 'refs/tags/staging/*')
    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.GH_TOKEN }}
          fetch-depth: 0

      - name: Run realse
        run: echo Hello, world!
